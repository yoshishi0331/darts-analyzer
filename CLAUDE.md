# ダーツフォーム解析アプリ 開発メモ

> 設計仕様の詳細は SPEC.md を参照。このファイルは開発経緯・決定事項・注意点をまとめたもの。

---

## 現在の開発状況

### 完了済み
- ステップ1：segmentのトグル削除（骨格ON/軌跡ON）
- ステップ2：UI整理 + 矢印キー追加（release/landing）
- ステップ3：targetステップ追加
- ステップ4：compareステップ追加 + ResearchCanvas接続
- ホーム画面：フォームランク表示・星表示・直近10件平均
- フィードバックロジック追加（scoring.ts）
- CalendarScreen：formScore=90のハードコード削除
- segmentステップ削除（SegmentScreen.tsx削除）
- targetステップ削除、SessionSetupScreen.tsx新規作成
  - 狙い選択（ブル/20/19/18/17/16/15）横グリッド
  - 測定項目ON/OFF（グルーピング/リリース安定/狙い精度）
  - バリデーション：最低1項目ON、狙い精度ON時は狙い選択必須
  - リリース安定OFFの場合は動画選択をスキップ
- 測定項目に応じたステップ動的生成（activeSteps）
- ステップ番号を動的表示（1/2、2/3など）
- スコア計算をscoring.tsに統一（HomeScreen・CalendarScreen共通）
- 測定した指標のみで重みを割り直すロジック実装
- getFeedback()にmeasuredMetrics追加、測定指標のみでフィードバック生成
- 設定画面にデータ全削除ボタン追加（確認ダイアログ・state同時リセット）
- グルーピングアルゴリズムをAppProviderとCompareScreenで統一（中央値ベース）
- scale値調整：グルーピング1.6→2.8、狙い精度2.0→3.2（リリース安定は変更なし）
- ランク閾値を全ファイルで1箇所に集約・統一
- ランク閾値引き上げ（SS:93, S:87, A+:79, A:71, B+:62, B:54）
- ホーム画面のランク表示を最新1セッションに変更

### 次にやること（優先順）
- 実機確認（フィードバック全条件・腕角度計測・各ステップ全般）
- フィードバック文言の煮詰め（実機で違和感があった箇所）
- スコアの違和感調整（グルーピング閾値・狙い精度scale値）
- UIレイアウト・文字サイズ調整
- アプリの使い方説明の実装

### 実機確認待ち・要調整
- グルーピングスコアの閾値（コード内 `// TODO: 閾値3は仮設定`）
- 狙い精度のscale値（コード内 `// TODO: 狙い精度のscaleは実機確認後に調整`）
- targetPoint座標のズレ（ボード外縁半径0.46は目視推定、±5%のズレあり）
- ~~腕の角度計算式（未解決）~~ → **解決済み**（下記「腕の角度測定機能」セクション参照）

### 放置・後回し
- カレンダーのランクバッジglow/shadowがセル外にはみ出すバグ（原因不明・放置）
- expo-avのdeprecation警告（SDK54で対応予定）
- リリース安定の外れ値ペナルティ → 後回し

---

## 解析フロー（再設計・確定）
```
ホーム
↓「解析を始める」
セッション設定画面（新規）
↓「動画を選ぶ」
動画選択
↓
release（測定項目にリリース安定がONの場合のみ）
↓
landing（測定項目にグルーピング or 狙い精度がONの場合のみ）
↓
compare（結果）
```

### 廃止
- segmentステップ（区間設定）→ 完全廃止
- targetステップ（狙い選択）→ セッション設定画面に移動

---

## セッション設定画面（新規実装）

解析開始時に1回だけ表示する設定画面。

### UI構成
```
「今日の練習設定」

─── 狙い ───
[ ブル ][ 20 ][ 19 ][ 18 ][ 17 ][ 16 ][ 15 ]
（横並びグリッド、1つ選択必須）

─── 測定項目 ───
☑ グルーピング      着弾点3投をタップ
☑ リリース安定      リリース点3投をタップ
☑ 狙い精度          狙いと着弾を比較
（全部ONがデフォルト）
※ 狙い精度をONにするには狙いの選択が必要

─── 下部固定 ───
[ 動画を選ぶ → ]（Primary、全幅）
```

### 仕様
- 狙い未選択 + 狙い精度ON → 「動画を選ぶ」ボタン無効
- 狙い未選択 + 狙い精度OFF → ボタン有効
- 測定項目は最低1つONでないとボタン無効
- 選択状態はセッション中のみ保持（AsyncStorage永続化不要）

---

## 各ステップのUI方針

### release（測定項目：リリース安定がONの場合のみ表示）
- ヘッダー：「リリース点を記録する」（ステップ番号は動的に変わるため省略）
- 動画下説明：「ダーツが指から離れた瞬間をタップしてください」
- ボタン配置：[1投目][2投目][3投目][◀][▶] / [1x][0.5x][0.25x] ▶再生 / 矢印キー / [戻る][次へ]
- 入力済み投番号はチェックマーク付き表示
- 十字キー上に「タップした点を微調整できます」表示

### landing（測定項目：グルーピング or 狙い精度がONの場合のみ表示）
- ヘッダー：「着弾点を記録する」
- 動画下説明：「ダーツが刺さった位置をタップしてください」
- 矢印キーあり（releaseと同仕様）
- 3投すべて入力済みでないと次へボタン無効

### compare
- ヘッダー：「比較・分析」
- ResearchCanvasを表示（視覚がメイン）
- スコア表示：測定した指標のみ表示（未測定は非表示）
- 「※ スコアは目安です」を小さく表示
- フィードバックエリア：スコア下にカード表示
- [戻る] [保存して終了]

---

## スコア計算の設計（再設計）

### セッション単体のランク
測定した指標のみで割り直して計算する。
```
測定指標が3つ：グルーピング×0.4 + リリース安定×0.4 + 狙い精度×0.2
測定指標が2つ（例：グルーピング+リリース安定）：グルーピング×0.5 + リリース安定×0.5
測定指標が1つ：その指標のスコアがそのままランクになる
```

### ホーム画面のランク
指標ごとに直近N件の平均を使って合成する。
- グルーピング：直近N件の平均（測定したセッションのみ）
- リリース安定：直近N件の平均（測定したセッションのみ）
- 狙い精度：直近N件の平均（測定したセッションのみ）
- データが1件もない指標は「--」表示、ランク計算から除外

---

## targetPointの座標マッピング

BoardCanvasの正規化座標系（左上0,0・右下1,1・中心0.5,0.5）基準。

| 狙い | x | y |
|---|---|---|
| ブル | 0.50 | 0.50 |
| 20 | 0.50 | 0.29 |
| 19 | 0.44 | 0.70 |
| 18 | 0.62 | 0.33 |
| 17 | 0.57 | 0.70 |
| 16 | 0.33 | 0.62 |
| 15 | 0.67 | 0.62 |

※ ボード外縁半径0.46は目視推定。実機確認後に微調整すること。

---

## グルーピングスコアのアルゴリズム
```
① 3点間の距離を3通り計算（d12, d13, d23）
② 中央値をベーススコアの基準にする
③ score = 100 - (中央値 × scale)、0〜100にclamp
④ if (最大距離 / 中央値 > 3):  // TODO: 閾値3は仮設定
     score = score × 0.85
```

---

## 矢印キーの仕様（release/landing共通）

- ▲◀▼▶ 十字配置
- 現在選択中の投番号の点を微調整
- 1タップあたり正規化座標で0.005移動
- タップのみ（押しっぱなし不要）

---

## 削除済み・廃止

- TrajectoryLines.tsx（削除済み）
- videoPoseAnalyzer.ts（削除予定）
- VideoPointInput.tsx（削除予定）
- 骨格ON/軌跡ONトグル（削除済み）
- elbowPoints（削除予定）
- segmentステップ（廃止・削除対象）
- targetステップ（廃止・セッション設定画面に統合）
- SegmentScreen.tsx（削除済み）
- TargetScreen.tsx（削除済み、セッション設定に統合）

---

---

## 90度計測機能 設計仕様（v1.1）

### 操作フロー
- リリースステップ内で測定方法を選択
- 選択肢：リリースポイントのみ / 角度のみ / 両方（推奨・精度UP）
- 肘は3投共通1回タップ、手首は3投分個別タップ

### スコア設計
- リリースポイントスコアをベースに角度で減点方式
- 90度超えの本数に応じてペナルティ加算

### 診断ロジック（角度×垂れの組み合わせ）
```
90度超え × 大幅下ズレ(0.25以上) → 「リリースタイミングが遅い可能性が高い」ペナルティ大
90度超え × 明確下ズレ(0.10〜)  → 「タイミングが影響してるかも」ペナルティ中
90度超え × やや下ズレ(0.02〜)  → 「不安定要因あり、様子見」軽ペナルティ
90度以内 × 垂れてる            → 「グリップ・フォロースルー等の別原因」誘導
角度未測定 × 垂れてる          → 「角度測定機能で確認してみよう」促す
```

### 垂れの閾値（正規化座標Y差分）
```
0.00〜0.02 → 誤差範囲
0.02〜0.10 → やや下ズレ
0.10〜0.25 → 明確な下ズレ
0.25以上   → 大幅下ズレ（3トリプル方向）
```

### 後方互換
- 角度データなしの既存セッションはリリースポイントスコア100%で計算
- 角度未測定は「--」表示

---

## 腕の角度測定機能 設計方針確定事項

### 角度の位置づけ（確定）
- スコアリングには影響させない
- 着弾が垂れたときの原因特定サポートとして使う
- 「参考指標」としてフィードバック文言の中で触れる程度に留める

### 角度の定義（確定・Phil Taylor分析動画に準拠）
- L字（肘90°のセット位置）→ 90°
- テイクバック → <90°（実測で約70°）
- リリース理想 → 90〜95°
- フォロースルー（腕伸ばし切り）→ ~170°

### タップフロー（確定）
- 肩 → 肘 → 手首の順に3点タップ（各フェーズでプレビュー→確定）
- 肩点は描画（VideoPlayerView/CompareScreenの2本線）専用

### 角度計算（確定）
**肘関節角**：（肘→肩）ベクトルと（肘→手首）ベクトルのなす角。

```typescript
// ReleaseScreen.tsx calcArmAngle
const v1x = (shoulder.x - elbow.x) * aspectRatio;
const v1y = shoulder.y - elbow.y;
const v2x = (wrist.x - elbow.x) * aspectRatio;
const v2y = wrist.y - elbow.y;
const dot = v1x * v2x + v1y * v2y;
const mag1 = Math.sqrt(v1x * v1x + v1y * v1y);
const mag2 = Math.sqrt(v2x * v2x + v2y * v2y);
return Math.acos(dot / (mag1 * mag2)) * 180 / Math.PI;
```

**なぜ肘関節角か：** 前腕と垂直線のなす角は撮影方向・カメラ角度に依存して狂う。肘関節角は3点の相対位置だけで決まるため安定。

### フィードバック閾値（暫定）
- ≤95° → 良いリリース（垂れにくい）
- 96〜105° → やや遅め
- >105° → リリースが遅い

---

## フィードバックロジック 設計方針確定事項

### 基本思想
- スコアリング：着弾位置・グルーピングベース
- 腕の角度：原因特定の補助情報としてフィードバック文言に組み込む
- 複数条件が該当する場合は全て表示
- 腕の角度が未測定の場合は①②③④をスキップ
- グリップ・矢角への言及は直近3セッション以上同じ傾向が続いた場合のみ
- イップス防止のためグリップへの言及は最小限に留める

### 表示条件と文言

**① 着弾垂れ ＋ 腕90度超え（単発）**
「少しリリースが遅めかもしれません。腕が伸びきる前に離すイメージで、次の1投試してみましょう！」

**② 着弾垂れ ＋ 腕90度以内（単発）**
「リリースのタイミングは悪くないです。グリップの力みや手首を引きすぎていないか、フォロースルーが下がってないかも確認してみましょう。」

**③ 着弾垂れ ＋ 腕90度超え（直近3セッション以上継続）**
※①より優先して表示、①は出さない
「最近、垂れる傾向が続いていますね。リリースが少し遅めの可能性があります。グリップで矢が立ち気味の方はとくに、腕が伸びきる前に離すと改善することが多いです。焦らず一緒に直していきましょう！」

**④ 着弾正常 ＋ 腕90度超え**
「今日の結果はバッチリです！ただ腕の角度が少し大きめなので、疲れてきたときに乱れやすいかも。余裕があるときに意識してみてください。」

**⑤ 着弾高め**
「全体的に高めに集まっていますね。リリースが少し早い可能性があります。もう少しだけ引きつけてから離すイメージで投げてみましょう！」

**⑥ 左右ブレ**
「縦のコントロールは良いですね！横のブレはリリース時に手首が左右にぶれている可能性があります。肘とボードを一直線にするイメージで投げてみましょう。」

**⑦ グルーピング良好 ＋ 着弾ズレ**
「3本の集まりはとても良いです！これはフォームが安定している証拠。あとはエイムの調整だけなので、狙う位置を少し補正してみましょう。フォームは自信持っていいです！」

**⑧ グルーピングバラバラ**
「着弾がバラついていますね。毎回同じフォームで投げることを最優先にしてみましょう。まずはスコアより『同じ動きを繰り返すこと』だけを意識するのが近道です。」

**⑨ 1本だけ極端にズレ**
「2本はいい感じです！1本だけズレるのはフォームより集中やリズムの乱れのことが多いです。深呼吸してルーティンを整えてみましょう。」

### フィードバックが0件の場合
「今日のフォームは安定しています！この調子で続けましょう！」

---

## このセッションでやったこと（2026-03-27）

1. **CompareScreenに腕角度表示エリア追加**（ArmAngleSection、react-native-svg）
2. **腕角度計測を2点→3点（肩・肘・手首）に変更**
   - タップフロー：肩→肘→手首の順、各フェーズでプレビュー→確定
   - VideoPlayerView：肩→肘→手首の2本線+3ドット描画
   - CompareScreen：実座標スケーリングで2本線+肘弧のSVG表示
   - types.ts：`shoulderPoints` を `DetailInputState` に追加
   - storage.ts：shoulderPoints の正規化処理追加
3. **角度計算ロジックを確定（肘関節角）**
   - 旧：前腕と垂直線のなす角（撮影角度に依存して不安定だった）
   - 新：（肘→肩）×（肘→手首）のなす角（肘関節角）
   - 実機確認済み：L字≈90°、テイクバック≈70°、リリース≈95°
4. **注釈追加**：CompareScreen腕角度エリアに「撮影角度により誤差があります」
5. **フィードバックロジック実装**（scoring.ts / CompareScreen.tsx）
   - 条件①〜⑨パターンの文言と判定ロジック実装
   - 腕角度閾値：97度
   - 腕角度未測定時は①②③④をスキップ
   - targetPoint未設定時は⑤⑦をスキップ
   - 実機確認はまだ

---

## 注意事項（開発全般）

- shadow/glow系は癖があるので過信しない
- 難しい・不確かなことを「可能」と言わない
- ストレージキー：`darts-analyzer/app-state/v1`（AsyncStorage、JSON一括）
- 起動コマンド：`cd C:\dev\darts-analyzer && npx expo start -c`
