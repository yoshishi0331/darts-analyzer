# ダーツフォーム解析アプリ 開発メモ

> 設計仕様の詳細は SPEC.md を参照。このファイルは開発経緯・決定事項・注意点をまとめたもの。

---

## 現在の開発状況

### 完了済み
- ステップ1：segmentのトグル削除（骨格ON/軌跡ON）
- ステップ2：UI整理 + 矢印キー追加（release/landing）
- ステップ3：targetステップ追加
- ステップ4：compareステップ追加 + ResearchCanvas接続
- ホーム画面：アプリ名・説明文・フォームランク表示・星表示に変更、今日の状況エリア削除
- ナビゲーション：「簡易」タブ削除、「詳細」→「フォーム解析」に変更
- 各ステップUI：説明文追加、ボタンラベル修正、余白・フォント統一
- compareスコア表示：数字＋星に統一
- フィードバックロジック追加（scoring.ts）
  - getFeedback() 関数・FeedbackInput・FeedbackResult 型を追加
  - リリース安定×グルーピングの4パターン＋方向別補足＋3投目ズレ判定を実装
- compareにフィードバック表示エリア追加（ResearchCanvas.tsx）
  - スコアエリア下・スクロール位置に「📋 フォームフィードバック」カード表示
  - getFeedback()がnullの場合は非表示
- LandingScreenの未入力ガード強化
  - 3投すべてのimpactPointがnullでない場合のみ「次へ」ボタンを有効化
- ホーム画面：3ボックスの順番を「狙い精度→リリース安定→グルーピング」に変更
- ホーム画面：フォームランクの集計を直近10件の平均に変更、サブタイトル更新
- targetステップから「指定なし」を削除（7択に変更）
- scoring.ts：狙いなし計算式を削除、フォームランクを3軸固定に統一
- CalendarScreen：formScore=90のハードコードを削除、aimAccuracyScoreを集計に追加

### 実機確認待ち・要調整
- グルーピングスコアの閾値（コード内 `// TODO: 閾値3は仮設定`）
- 狙い精度のscale値（コード内 `// TODO: 狙い精度のscaleは実機確認後に調整`）
- targetPoint座標のズレ（ボード外縁半径0.46は目視推定、±5%のズレあり）

### 放置・後回し
- カレンダーのランクバッジglow/shadowがセル外にはみ出すバグ（原因不明・放置）
- expo-avのdeprecation警告（SDK54で対応予定）
- 一言フィードバック機能（「リリース位置が安定すればS狙えます」など）
- 簡易タブへの新機能割り当て

---

## 詳細解析のフロー（確定）

```
segment（1/5）リリース区間を設定
↓
release（2/5）リリース点を記録する
↓
target（3/5）狙いを選択
↓
landing（4/5）着弾点を記録する
↓
compare（5/5）比較・分析
```

---

## 各ステップのUI方針

### segment
- ヘッダー：「ステップ 1/5　リリース区間を設定」（1行のみ）
- 「詳細解析」タイトル・説明文はsegmentのみ表示、release以降は非表示
- 言葉：開始点→セットアップ、終了点→リリース後
- 説明文：「ダーツを投げる「開始〜リリース後」までを指定します」
- TrajectoryLines（3本カラー棒線）削除済み

### release
- ヘッダー：「ステップ 2/5　リリース点を記録する」
- 動画下説明：「ダーツが指から離れた瞬間をタップしてください」
- 骨格・軌跡ボタン削除済み
- ボタン配置：[1投目][2投目][3投目][◀][▶] / [1x][0.5x][0.25x] ▶再生 / 矢印キー / [戻る][次へ]
- 入力済み投番号はチェックマーク付き表示
- 十字キー上に「タップした点を微調整できます」表示

### target
- ヘッダー：「ステップ 3/5　狙いを選択」
- 説明文：「今回狙った場所を選んでください」
- 選択肢：ブル / 20 / 19 / 18 / 17 / 16 / 15（横2列グリッド、7択）
- 選択後に「選択中：〇〇」フィードバック表示あり
- 未選択時は次へボタン無効

### landing
- ヘッダー：「ステップ 4/5　着弾点を記録する」
- 動画下説明：「ダーツが刺さった位置をタップしてください」
- 矢印キーあり（releaseと同仕様）
- 3投すべて入力済みでないと次へボタン無効

### compare
- ヘッダー：「ステップ 5/5　比較・分析」
- ResearchCanvasを表示（視覚がメイン）
- スコア表示：グルーピング / リリース安定 / 狙い精度（数字＋星）
- 「⚠ 1投だけ外れています」フラグ表示あり
- 「※ スコアは目安です」を小さく表示
- フィードバックエリア：スコア下にカード表示（getFeedback()がnullなら非表示）
- [戻る] [保存して終了]

---

## targetPointの座標マッピング

BoardCanvasの正規化座標系（左上0,0・右下1,1・中心0.5,0.5）基準。

| 狙い | x | y |
|---|---|---|
| ブル | 0.50 | 0.50 |
| 20 | 0.50 | 0.29 |
| 19 | 0.44 | 0.70 |
| 18 | 0.62 | 0.33 |
| 17 | 0.57 | 0.70 |
| 16 | 0.33 | 0.62 |
| 15 | 0.67 | 0.62 |
※ ボード外縁半径0.46は目視推定。実機確認後に微調整すること。

---

## グルーピングスコアのアルゴリズム

```
① 3点間の距離を3通り計算（d12, d13, d23）
② 中央値をベーススコアの基準にする
③ score = 100 - (中央値 × scale)、0〜100にclamp
④ if (最大距離 / 中央値 > 3):  // TODO: 閾値3は仮設定
     score = score × 0.85
     1投ミスフラグ = true
```

---

## スコア計算式

```
グルーピング × 0.4 ＋ リリース安定 × 0.4 ＋ 狙い精度 × 0.2
```

---

## フィードバックロジック（scoring.ts）

getFeedback(input: FeedbackInput): FeedbackResult | null

- boardHitsに {x:0.5, y:0.5} が含まれる場合はnullを返す（未入力フォールバック検出）
- 判定優先順位：3投目ズレ → パターンA/B/C/D → 方向別補足
- 出力：{ main: string, direction: string | null }

---

## 矢印キーの仕様（release/landing共通）

- ▲◀▼▶ 十字配置
- 現在選択中の投番号の点を微調整
- 1タップあたり正規化座標で0.005移動
- タップのみ（押しっぱなし不要）

---

## 削除済みコンポーネント

- TrajectoryLines.tsx（削除）
- videoPoseAnalyzer.ts（削除予定）
- VideoPointInput.tsx（削除予定）
- 骨格ON/軌跡ONトグル（削除済み）
- elbowPoints（削除予定）

---

## 注意事項（開発全般）

- shadow/glow系は癖があるので過信しない
- 難しい・不確かなことを「可能」と言わない
- 抽象的な質問（「どれがいい？」など）はせず、具体的な画面・数値で聞く
- 「次のチャットに行く」と言ったらCLAUDE.mdの更新を提案する
- ストレージキー：`darts-analyzer/app-state/v1`（AsyncStorage、JSON一括）
- 起動コマンド：`cd C:\dev\darts-analyzer && npx expo start -c`

---

## claude.ai引継ぎ用メモ

### 未確認・要調整
- グルーピングスコアの閾値（コード内 `// TODO: 閾値3は仮設定`）
- 狙い精度のscale値（実機確認後に調整）
- targetPoint座標のズレ（ボード外縁半径0.46は目視推定）
- 計算の二重管理（CompareとAppProviderで別々に計算）→scale値調整時に一緒に解消

### 次にやること候補
- scale値の実機確認・調整
- ホーム画面の細かいUI調整
- その他実機で気になった点

### claude.aiへの注意事項
- 難しい・不確かなことを「可能」と言わない
- shadow/glow系は癖があるので過信しない
- 抽象的な質問はせず、具体的な画面・数値で聞く
- 「次のチャットに行く」と言ったらCLAUDE.mdの更新を提案する
